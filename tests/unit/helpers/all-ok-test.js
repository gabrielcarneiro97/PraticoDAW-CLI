import { allOk } from 'pratico-daw-cli/helpers/all-ok';
import { module, test } from 'qunit';

module('Unit | Helper | all ok');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = allOk([42]);
  assert.ok(result);
});
