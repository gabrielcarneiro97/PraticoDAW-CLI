import { allOkEmpresa } from 'pratico-daw-cli/helpers/all-ok-empresa';
import { module, test } from 'qunit';

module('Unit | Helper | all ok empresa');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = allOkEmpresa([42]);
  assert.ok(result);
});
